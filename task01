"""1. Реализовать класс «Дата», функция-конструктор которого должна принимать дату в виде
строки формата «день-месяц-год». В рамках класса реализовать два метода. Первый, с
декоратором @classmethod, должен извлекать число, месяц, год и преобразовывать их тип к типу
«Число». Второй, с декоратором @staticmethod, должен проводить валидацию числа, месяца и года
(например, месяц — от 1 до 12). Проверить работу полученной структуры на реальных данных."""


class Data:
    def __init__(self, date):
        self.date = date

    def extract_data(self):
        try:
            date, month, year = self.date.split()
            return date, month, year
        except Exception as e:
            print(f'не удалось выделить дату из строки {e}')
    @staticmethod
    def validate_data(date_input):
        try:
            date, month, year = date_input
            if int(date) not in range(1, 32):
                raise ValueError('неверно указан день')
            elif int(month) not in range(1, 13):
                raise ValueError('неверно указан месяц')
            elif int(year) not in range(0,3001):
                raise ValueError('неверно указан год')
        except ValueError as e:
            print(e)
        else:
            print('дата указана верно')

my_date_cls = Data('03 12 2000')
my_date = my_date_cls.extract_data()
print(my_date)
if my_date:
    my_date_cls.validate_data(my_date)
