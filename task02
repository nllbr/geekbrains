"""
2. Реализовать функцию, принимающую несколько параметров, описывающих данные пользователя:
имя, фамилия, год рождения, город проживания, email, телефон. Функция должна принимать параметры
 как именованные аргументы. Реализовать вывод данных о пользователе одной строкой.
"""

def data():
    while True:
        check_inputs = input('ввести данные пользователя д/н: ')
        if check_inputs == 'д':
            name = input('введите ваше имя: ')
            surname = input('введите ваше фамилию: ')
            try:
                birth_year = int(input('введите ваш год рождения: '))
            except ValueError:
                print('неверный формат')
                birth_year = int(input('введите ваш год рождения: '))
            city = input('введите ваш город проживания: ')
            email = input('введите ваш e-mail: ')
            phone = input('введите ваш номер телефона: ')
            print(f'{name} {surname}, {2021 - birth_year} лет, г. {city}, {email}, {phone}')
        elif check_inputs == 'н':
            print('ввод данных окончен')
            break
        else:
            sec_try = input('вы ввели некорректные данные, попробовать еще раз д/н: ')
            if sec_try == 'д':
                check_inputs
            else:
                return


data()
