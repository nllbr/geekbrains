"""2. Создать текстовый файл (не программно), сохранить в нем несколько строк,
выполнить подсчет количества строк, количества слов в каждой строке."""

count = 0
with open("text_2", "r") as f_obj:
    for line in f_obj:
        count += 1
print(f'количество строк равно {count}')
